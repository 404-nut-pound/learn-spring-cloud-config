server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file://D:/dev/workspace_code/learn-spring-cloud/learn-spring-cloud-config-repo
